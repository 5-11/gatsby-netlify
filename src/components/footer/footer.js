import React from 'react';

const Footer = ({ data }) => {
    return (
        <footer className="footer">
            <div className="social-links">

            </div>
        </footer>
    );
};

export default Footer;